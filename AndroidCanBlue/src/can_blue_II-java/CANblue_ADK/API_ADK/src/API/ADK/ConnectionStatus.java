package API.ADK;

/**
 * Enum of the Instance status.
 * @author hroesdiyono
 */
public enum ConnectionStatus
{
  /** The instance is not created. */
  INSTANCE_NOT_CREATED,
  /** The instance is created. */
  INSTANCE_CREATED,
}